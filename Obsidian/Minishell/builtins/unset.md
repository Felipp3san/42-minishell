Removes an environment variable or shell variable.
___
## Usage Examples

```bash
unset MYVAR    # removes MYVAR from the environment
```

In a shell, `echo` is often used for simple output or debugging.